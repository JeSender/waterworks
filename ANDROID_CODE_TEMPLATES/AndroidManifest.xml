<?xml version="1.0" encoding="utf-8"?>
<!--
    Android Manifest for Balilihan Waterworks App

    Location: app/src/main/AndroidManifest.xml

    IMPORTANT CONFIGURATIONS:
    - Network security config for HTTPS
    - Internet permission
    - Network state permission
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==================== PERMISSIONS ==================== -->

    <!-- Required for making API requests -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required for checking network connectivity -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Optional: For location-based features (if needed) -->
    <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> -->
    <!-- <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BalilihanWaterworks"
        tools:targetApi="31"

        <!-- IMPORTANT: Network Security Configuration -->
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <!-- Main/Splash Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BalilihanWaterworks">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.BalilihanWaterworks.NoActionBar" />

        <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- Consumer List Activity -->
        <activity
            android:name=".ConsumerListActivity"
            android:exported="false" />

        <!-- Meter Reading Activity -->
        <activity
            android:name=".MeterReadingActivity"
            android:exported="false" />

    </application>

</manifest>
